== knot_name
weave 'foo
| > Option 1
    -> dest1 -- Comment allowed here and ignored
| > Option 2 -- Comment included in string
    -> dest2
| _
    -> dest_default
;;

== dest1
> dest1 is a real knot

== dest2(A, B, C)
> dest2 wants some arguments and we're not going to give them to it

== dest_default
-> knot_name
